extends layout

block content
  title= title || 'Error'

  section.error-section
    .error-container
      h2 #{statusCode || 'Error'}! Error
      p.error-message #{message || 'Something went wrong.'}
      a.error-link(href="/") Return to Homepage
